/*// Read in inputs from file.
	char c;
	int j;
	char** data;
	data = new char*[100];
	for (int i = 0; i < 100; i++)
	{
		data[i] = new char[100];
	}
	for (int i = 0; i < 100; i++)
	{
		// Reads the first character.
		cin.get(c);
		
		// Resets the j index.
		j = 0;
		do
		{
			data[i][j++] = c;
			cin.get(c);

		} while ((c != ' ') && (c != '\n'));

		data[i][j] = '\0';
	}*/
